<div class="header" >
<h1>Bienvenue ! </h1>
<h2>Liste des taches à faire</h2>
<br>


<%=link_to "Crée une nouvelle tache", new_task_path, {:class => "nique"} %> <br> <br> <br>

<% @tasks.each do |task| %>
  <%= task.done ? "[X]" : "[ ]" %> <%=link_to task.name, task_path(task), {:class => "tasks"} %> <br>
  <%= link_to "DELETE", task_path(task),
    method: :delete,
    class: "action",
    data: { confirm: "Are you sure?" } %>
  <%= link_to "UPDATE", edit_task_path(task), {:class => "action"} %> <br> <br>
<% end %>
</div>

